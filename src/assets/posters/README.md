Solte aqui as imagens dos pôsteres da parede do fliperama (jpg/png/webp, retrato, ~700 px de largura basta).
Qualquer arquivo nesta pasta aparece automaticamente na parede (`Arcade.tsx` usa `import.meta.glob`).
Sem arquivos, a parede usa pôsteres geométricos gerados por código.
